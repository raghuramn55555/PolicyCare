# PolicySetu Backend — Environment Variables

# Server port (default: 3000)
PORT=3000

# OpenAI API Key (optional — mock embeddings used if not set)
OPENAI_API_KEY=

# Log level: DEBUG, INFO, WARN, ERROR (default: INFO)
LOG_LEVEL=INFO
